# find

import { Sandpack } from '@codesandbox/sandpack-react'

## substring

<Sandpack
  template='vanilla'
   options={{ showConsole: true }} 
  files={{
    '/index.js':{
      code:`
      const a = '123'
      const b = a.substring()

      console.log(a)
      console.log(b)
      `
    },

}}
/>
